# This is the homogeneous transformation matrix repo with rust

Just download this and run `cargo run`, you'll get the matrix you need
to make your own rotation, just check the docs and the example code
